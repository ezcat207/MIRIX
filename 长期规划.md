1 核心目标：提高 ai 人 sync 同步率
2 信任，回顾，只存链接
base_memory


# 在相应的 ORM 模型中添加  
source_url: Optional[str]  # OCR 提取的 URL  
source_app: Optional[str]  # 应用名称  
screenshot_path: Optional[str]  # 截图路径  
ocr_text: Optional[str]  # OCR 提取的完整文本

在各个记忆类型（Episodic、Semantic 等）的数据库表中添加元数据字段：

